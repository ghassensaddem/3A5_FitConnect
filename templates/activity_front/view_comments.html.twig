<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commentaires - {{ salle.nomSalle }}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #28a745;
            --primary-light: #5cb85c;
            --primary-dark: #218838;
            --secondary: #007bff;
            --dark: #2c3e50;
            --darker: #1a252f;
            --light: #f8f9fa;
            --lighter: #ffffff;
            --gray: #95a5a6;
            --gray-light: #ecf0f1;
            --danger: #e74c3c;
            --danger-dark: #c0392b;
            --success-light: #d4edda;
            --success-border: #c3e6cb;
            --box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
        }
        
        body {
            background-color: #f5f7fa;
            color: #34495e;
            line-height: 1.7;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .container {
            max-width: 1000px;
            width: 95%;
            margin: 40px auto;
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: var(--box-shadow);
            flex: 1;
        }
        
        .header {
            background: linear-gradient(135deg, var(--dark) 0%, var(--darker) 100%);
            color: white;
            padding: 2.5rem 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 70%);
            transform: rotate(30deg);
        }
        
        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 0.75rem;
            font-weight: 700;
            position: relative;
        }
        
        .header p {
            opacity: 0.9;
            font-size: 1.2rem;
            font-weight: 300;
            position: relative;
            max-width: 600px;
            margin: 0 auto;
        }
        
        .content {
            padding: 2.5rem;
        }
        
        .comment-list {
            margin-top: 2.5rem;
        }
        
        .comment-card {
            padding: 2rem;
            margin-bottom: 2rem;
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            border-left: 5px solid var(--primary);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }
        
        .comment-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }
        
        .comment-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(40, 167, 69, 0.03) 0%, rgba(255,255,255,0) 100%);
            pointer-events: none;
        }
        
        .comment-header {
            display: flex;
            align-items: center;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
            gap: 1.5rem;
        }
        
        .comment-stars {
            color: var(--primary);
            font-size: 1.3rem;
            display: flex;
            gap: 0.3rem;
        }
        
        .comment-author {
            font-weight: 700;
            color: var(--dark);
            font-size: 1.1rem;
        }
        
        .comment-date {
            color: var(--gray);
            font-size: 0.9rem;
            margin-left: auto;
            background: var(--gray-light);
            padding: 0.3rem 0.8rem;
            border-radius: 50px;
            font-weight: 600;
        }
        
        .comment-content {
            color: #495057;
            line-height: 1.8;
            font-size: 1.05rem;
            padding-right: 1rem;
        }
        
        .comment-content:before {
            content: open-quote;
            font-size: 3rem;
            line-height: 0;
            color: var(--gray-light);
            margin-right: 0.5rem;
            vertical-align: -0.5rem;
        }
        
        .no-comments {
            text-align: center;
            padding: 3rem;
            background: var(--light);
            border-radius: 12px;
            border: 2px dashed #dee2e6;
            transition: var(--transition);
        }
        
        .no-comments:hover {
            background: white;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }
        
        .no-comments i {
            font-size: 3.5rem;
            color: var(--gray);
            margin-bottom: 1.5rem;
            opacity: 0.7;
        }
        
        .no-comments p {
            font-size: 1.2rem;
            color: var(--gray);
            margin-bottom: 0;
            font-weight: 500;
        }
        
        .actions {
            display: flex;
            justify-content: space-between;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid #e9ecef;
        }
        
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 1rem 2rem;
            border-radius: 50px;
            font-weight: 600;
            text-decoration: none;
            transition: var(--transition);
            cursor: pointer;
            border: none;
            font-size: 1.05rem;
            line-height: 1.5;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .btn-primary {
            background: var(--primary);
            color: white;
        }
        
        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(40, 167, 69, 0.3);
        }
        
        .btn-primary:active {
            transform: translateY(0);
        }
        
        .btn-secondary {
            background: var(--primary);
            color: white;
        }
        
        .btn-secondary:hover {
            background: var(--primary-dark);
            transform: translateY(-3px);
            box-shadow: 0 6px 12px rgba(40, 167, 69, 0.3);
        }
        
        .btn i {
            margin-right: 0.75rem;
            font-size: 1.1em;
        }
        
        @media (max-width: 768px) {
            .container {
                margin: 20px auto;
                border-radius: 0;
                width: 100%;
            }
            
            .header {
                padding: 2rem 1.5rem;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .header p {
                font-size: 1.1rem;
            }
            
            .content {
                padding: 1.5rem;
            }
            
            .comment-card {
                padding: 1.5rem;
            }
            
            .comment-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
            
            .comment-date {
                margin-left: 0;
            }
            
            .actions {
                flex-direction: column;
                gap: 1rem;
            }
            
            .btn {
                width: 100%;
                justify-content: center;
            }
        }
        
        @media (max-width: 480px) {
            .header h1 {
                font-size: 1.8rem;
            }
            
            .comment-stars {
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Commentaires sur {{ salle.nomSalle }}</h1>
            <p>Retours des utilisateurs sur cette salle</p>
        </div>
        
        <div class="content">
            {% if ratings|length > 0 %}
                <div class="comment-list">
                    {% for rating in ratings %}
                        <div class="comment-card">
                            <div class="comment-header">
                                <div class="comment-stars">
                                    {% for i in 1..5 %}
                                        <i class="fas fa-star {{ i <= rating.ratingStars ? 'active' : '' }}"></i>
                                    {% endfor %}
                                </div>
                                <span class="comment-author">{{ rating.client.nom }} {{ rating.client.prenom }}</span>
                                <span class="comment-date">{{ rating.ratingStars }} étoiles</span>
                            </div>
                            <div class="comment-content">
                                {{ rating.review ? rating.review : "Cet utilisateur n'a pas laissé de commentaire." }}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                <div class="no-comments">
                    <i class="far fa-comment-dots"></i>
                    <p>Aucun commentaire n'a été posté pour cette salle.</p>
                </div>
            {% endif %}
            
            <div class="actions">
                <a href="{{ path('rate_salle', {'activityId': activity.idActivity, 'salleId': salle.idSalle}) }}" class="btn btn-primary">
                    <i class="fas fa-star"></i> Donner votre avis
                </a>
                <a href="javascript:history.back()" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour
                </a>
            </div>
        </div>
    </div>
</body>
</html>