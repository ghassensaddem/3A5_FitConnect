
{% extends 'base.html.twig' %}
{% block title %}Typeactivite{% endblock %}

{% block body %}
    <div class="container custom-page"> 
        <h1>Typeactivite</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ typeactivite.id }}</td>
                </tr>
                <tr>
                    <th>Title</th>
                    <td>{{ typeactivite.title }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ typeactivite.description }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_typeactivite_index') }}">back to list</a>
        <a href="{{ path('app_typeactivite_edit', {'id': typeactivite.id}) }}">edit</a>

        {{ include('typeactivite/_delete_form.html.twig') }}
    </div>  
{% endblock %}
