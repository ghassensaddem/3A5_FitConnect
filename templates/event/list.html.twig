<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>📅 Liste des Événements</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Ajout de FontAwesome pour les icônes -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="{{ asset('css/custom.css') }}">
    <style>
        .fancy-title {
            font-size: 2.8rem;
            font-weight: bold;
            text-align: center;
            padding: 20px;
            border-radius: 15px;
            background: linear-gradient(135deg, #f1f4f9, #dfe9f3);
            color: #2c3e50;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
            position: relative;
            animation: fadeInDown 1s ease-out;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fancy-title::before {
            content: '✨';
            position: absolute;
            left: 15px;
            top: 15px;
            font-size: 1.5rem;
        }

        .fancy-title::after {
            content: '🎉';
            position: absolute;
            right: 15px;
            top: 15px;
            font-size: 1.5rem;
        }

        /* ---- Barre de recherche ---- */
        .search-form .input-group {
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        .search-form .form-control {
            border: none;
            min-width: 200px;
            height: 40px;
        }

        .search-form .input-group-text {
            background-color: #003f44;
            color: white;
            border: none;
        }

        /* Select stylé */
        .sort-select {
            border-radius: 12px;
            padding: 8px 12px;
            background-color: #003f44;
            color: white;
            border: none;
            font-weight: bold;
            height: 40px;
            min-width: 120px;
        }

        /* Focus effet */
        .sort-select:focus {
            outline: none;
            box-shadow: 0 0 5px rgba(0, 63, 68, 0.5);
        }

        .custom-btn {
            background-color: transparent;
            color: #053536;
            font-weight: bold;
            border-radius: 10px;
            padding: 8px 14px;
            border: none;
            transition: 0.3s ease;
            text-decoration: none;
        }

        .custom-btn:hover {
            background-color: #053536;
            color: white;
        }

        /* Barre de recherche */
        .search-form .input-group {
            border-radius: 10px;
            overflow: hidden;
        }

        .search-form .form-control {
            border: none;
            min-width: 180px;
            height: 40px;
        }

        .input-group-text {
            background-color: #003f44;
            border: none;
        }

        /* ChoiceBox style */
        .custom-select {
            background-color: #003f44;
            color: white;
            font-weight: bold;
            border-radius: 10px;
            padding: 8px;
            border: none;
            height: 40px;
            min-width: 130px;
        }

        .d-flex {
            display: flex;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
        }

        @media (min-width: 768px) {
            .d-flex {
                flex-wrap: nowrap;
            }
        }

        .custom-card-header {
            background-color: #053536;
            color: #F1F2F6;
            font-weight: bold;
            padding: 20px 30px;  /* Augmenter la taille du header */
            border-top-left-radius: 0.25rem;
            border-top-right-radius: 0.25rem;
            font-size: 1.5rem;  /* Augmenter la taille de la police */
            text-align: center;  /* Centrer le texte */
        }

        .custom-card {
            transition: transform 0.4s ease-in-out, box-shadow 0.4s ease-in-out; /* Transition plus longue pour un effet plus visible */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Ombre légère sur chaque carte */
            border-radius: 8px; /* Coins arrondis de la carte */
        }

        .custom-card:hover {
            transform: translateY(-20px); /* Augmenter le mouvement vers le haut */
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.25); /* Ombre plus grande et plus nette */
            border: 3px solid #053536; /* Bordure plus épaisse et colorée */
        }

        .custom-image {
            border-radius: 0;  /* Supprime la bordure arrondie */
        }
    </style>

</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3 sidebar">
                {% include '_sidebar.html.twig' %}
            </div>

            <!-- Contenu principal -->
            <div class="col-md-9 main-content">
                <!-- Groupe contenant recherche + tri -->
                <div class="d-flex align-items-center gap-2 flex-nowrap mb-4 px-3 justify-content-start" style="overflow-x: auto;">
                    <!-- Barre de recherche -->
                    <form method="get" class="d-flex align-items-center me-2">
                        <div class="input-group">
                            <span class="input-group-text bg-dark text-white"><i class="fas fa-search"></i></span>
                            <input type="text" name="search" class="form-control" placeholder="Rechercher..." value="{{ app.request.query.get('search') }}">
                        </div>
                    </form>
                    <a href="#" class="btn custom-btn">Réserver</a>
                    <a href="#" class="btn custom-btn">Plus d'info</a>
                    <a href="#" class="btn custom-btn">Contact</a>
                    <a href="#" class="btn custom-btn">Souvenirs</a>
                    <!-- Boutons avec icônes -->
                    <a href="#" class="btn custom-btn" onclick="playSound()"><i class="fas fa-volume-up"></i></a>  <!-- Mettre Son -->
                    <a href="#" class="btn custom-btn" onclick="pauseSound()"><i class="fas fa-volume-mute"></i></a>  <!-- Enlever Son -->

                    <!-- Tri -->
                    <form method="get" class="ms-2">
                        <select name="sort" class="form-select custom-select" onchange="this.form.submit()">
                            <option value="date" {{ app.request.query.get('sort') == 'date' ? 'selected' }}>📅 Date</option>
                            <option value="prixdupass" {{ app.request.query.get('sort') == 'prixdupass' ? 'selected' }}>💰 Prix</option>
                            <option value="lieu" {{ app.request.query.get('sort') == 'lieu' ? 'selected' }}>📍 Lieu</option>
                        </select>
                    </form>
                </div>

                <h1 class="fancy-title">📅 Nos Événements </h1>

                <!-- Grille des événements -->
                <div class="row row-cols-1 row-cols-md-3 g-4">
                    {% for event in events %}
                        <div class="col">
                            <div class="card shadow-sm h-100 custom-card">
                                <!-- Entête personnalisée -->
                                <div class="custom-card-header">
                                    Date: 📅 {{ event.date ?: 'Non définie' }}
                                </div>

                                <img src="{{ asset(event.image ?: 'images/default-event.jpeg') }}" class="card-img-top custom-image" alt="Image de l'événement">

                                <div class="card-body">
                                    <p class="card-text" style="font-size: 1.2rem; color: #333; line-height: 1.6; font-family: 'Arial', sans-serif;">
                                        📍 <strong style="color: #053536; font-size: 1.4rem;">Lieu :</strong> 
                                        <span style="font-style: italic;">{{ event.lieu ?: 'Non spécifié' }}</span><br>
                                        💰 <strong style="color: #053536; font-size: 1.4rem;">Prix :</strong> 
                                        <span style="font-weight: bold; color: noire;">
                                            {{ event.prixdupass > 0 ? event.prixdupass ~ ' TND' : 'Gratuit' }}
                                        </span>
                                    </p>
          <a href="{{ path('event_details', { id: event.id }) }}" class="btn btn-outline-success">
    Voir les activités
</a>


                                </div>

                            </div>
                        </div>
                    {% else %}
                        <p class="text-center text-muted">😔 Aucun événement trouvé.</p>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

    <!-- Éléments audio -->
    <audio id="audioPlayer" src="/event.mp3" preload="auto"></audio>
    <audio id="audioPlayer" src="/event.mp3" preload="auto"></audio>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JavaScript pour contrôler le son -->
    <script>
        function playSound() {
            var audioPlayer = document.getElementById("audioPlayer");
            audioPlayer.play();
        }

        function pauseSound() {
            var audioPlayer = document.getElementById("audioPlayer");
            audioPlayer.pause();
        }
    </script><audio id="audioPlayer" src="/event.mp3" preload="auto"></audio>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JavaScript pour contrôler le son -->
    <script>
        function playSound() {
            var audioPlayer = document.getElementById("audioPlayer");
            audioPlayer.play();
        }

        function pauseSound() {
            var audioPlayer = document.getElementById("audioPlayer");
            audioPlayer.pause();
        }
    </script>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
   
</body>
</html>
