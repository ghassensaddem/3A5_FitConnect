{% extends 'base.html.twig' %}

{% block body %}
<div class="d-flex align-items-center gap-2 flex-nowrap mb-4 px-3 justify-content-start" style="overflow-x: auto;">
                    <!-- Barre de recherche -->
                    <form method="get" class="d-flex align-items-center me-2">
                        <div class="input-group">
                            <span class="input-group-text bg-dark text-white"><i class="fas fa-search"></i></span>
                            <input type="text" name="search" class="form-control" placeholder="Rechercher..." value="{{ app.request.query.get('search') }}">
                        </div>
                    </form>
                    <a href="#" class="btn custom-btn">Réserver</a>
                    <a href="#" class="btn custom-btn">Plus d'info</a>
                    <a href="#" class="btn custom-btn">Contact</a>
                    <a href="#" class="btn custom-btn">Souvenirs</a>
                    <!-- Boutons avec icônes -->
                    <a href="#" class="btn custom-btn" onclick="playSound()"><i class="fas fa-volume-up"></i></a>  <!-- Mettre Son -->
                    <a href="#" class="btn custom-btn" onclick="pauseSound()"><i class="fas fa-volume-mute"></i></a>  <!-- Enlever Son -->

                    <!-- Tri -->
                    <form method="get" class="ms-2">
                        <select name="sort" class="form-select custom-select" onchange="this.form.submit()">
                            <option value="date" {{ app.request.query.get('sort') == 'date' ? 'selected' }}>📅 Date</option>
                            <option value="prixdupass" {{ app.request.query.get('sort') == 'prixdupass' ? 'selected' }}>💰 Prix</option>
                            <option value="lieu" {{ app.request.query.get('sort') == 'lieu' ? 'selected' }}>📍 Lieu</option>
                        </select>
                    </form>
                </div>
                <h1 class="fancy-title">📅 Nos Activités </h1>
    <div class="container mt-5">
        <h2 class="text-primary mb-4">📍 Détails de l'événement : <strong>{{ event.lieu }}</strong></h2>
        <p><i class="bi bi-calendar-event"></i> <strong>Date :</strong> {{ event.date|date('d/m/Y') }}</p>
        <p><i class="bi bi-cash-coin"></i> <strong>Prix :</strong> {{ event.prixdupass }} TND</p>

        <h3 class="mt-4 mb-3 text-success">🎯 Activités proposées</h3>

        {% if activities is not empty %}
            {% for activity in activities %}
                <div class="card shadow-sm mb-3">
                    <div class="card-body">
                        {% set type = null %}
                        {% for t in typeactivites %}
                            {% if activity.typeactivite and t.id == activity.typeactivite.id %}
                                {% set type = t %}
                            {% endif %}
                        {% endfor %}

                        <h5 class="card-title text-primary">
                            {{ type ? type.title : 'Type inconnu' }}
                        </h5>
                        <p class="card-text text-muted fst-italic">
                            {{ type ? type.description : 'Pas de description' }}
                        </p>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">
                                🕒 <strong>Horaire :</strong> {{ activity.horaire }}
                            </li>
                            <li class="list-group-item">
                                👥 <strong>Participants :</strong> {{ activity.nbrparticipant }}
                            </li>
                              <li class="list-group-item text-center">
    <a href="{{ path('app_reservation_increment', {'id': activity.id}) }}" class="btn btn-success">
    ➕ Réserver
</a>

</li>

                        </ul>
                    </div>
                </div>
            {% endfor %}
        {% else %}
            <div class="alert alert-warning mt-3">
                Aucune activité trouvée pour cet événement.
            </div>
        {% endif %}
    </div>
        <style>
        .fancy-title {
            font-size: 2.8rem;
            font-weight: bold;
            text-align: center;
            padding: 20px;
            border-radius: 15px;
            background: linear-gradient(135deg, #f1f4f9, #dfe9f3);
            color: #2c3e50;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
            position: relative;
            animation: fadeInDown 1s ease-out;
        }

        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fancy-title::before {
            content: '✨';
            position: absolute;
            left: 15px;
            top: 15px;
            font-size: 1.5rem;
        }

        .fancy-title::after {
            content: '🎉';
            position: absolute;
            right: 15px;
            top: 15px;
            font-size: 1.5rem;
        }

        /* ---- Barre de recherche ---- */
        .search-form .input-group {
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        .search-form .form-control {
            border: none;
            min-width: 200px;
            height: 40px;
        }

        .search-form .input-group-text {
            background-color: #003f44;
            color: white;
            border: none;
        }

        /* Select stylé */
        .sort-select {
            border-radius: 12px;
            padding: 8px 12px;
            background-color: #003f44;
            color: white;
            border: none;
            font-weight: bold;
            height: 40px;
            min-width: 120px;
        }

        /* Focus effet */
        .sort-select:focus {
            outline: none;
            box-shadow: 0 0 5px rgba(0, 63, 68, 0.5);
        }

        .custom-btn {
            background-color: transparent;
            color: #053536;
            font-weight: bold;
            border-radius: 10px;
            padding: 8px 14px;
            border: none;
            transition: 0.3s ease;
            text-decoration: none;
        }

        .custom-btn:hover {
            background-color: #053536;
            color: white;
        }

        /* Barre de recherche */
        .search-form .input-group {
            border-radius: 10px;
            overflow: hidden;
        }

        .search-form .form-control {
            border: none;
            min-width: 180px;
            height: 40px;
        }

        .input-group-text {
            background-color: #003f44;
            border: none;
        }

        /* ChoiceBox style */
        .custom-select {
            background-color: #003f44;
            color: white;
            font-weight: bold;
            border-radius: 10px;
            padding: 8px;
            border: none;
            height: 40px;
            min-width: 130px;
        }

        .d-flex {
            display: flex;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
        }

        @media (min-width: 768px) {
            .d-flex {
                flex-wrap: nowrap;
            }
        }

        .custom-card-header {
            background-color: #053536;
            color: #F1F2F6;
            font-weight: bold;
            padding: 20px 30px;  /* Augmenter la taille du header */
            border-top-left-radius: 0.25rem;
            border-top-right-radius: 0.25rem;
            font-size: 1.5rem;  /* Augmenter la taille de la police */
            text-align: center;  /* Centrer le texte */
        }

        .custom-card {
            transition: transform 0.4s ease-in-out, box-shadow 0.4s ease-in-out; /* Transition plus longue pour un effet plus visible */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Ombre légère sur chaque carte */
            border-radius: 8px; /* Coins arrondis de la carte */
        }

        .custom-card:hover {
            transform: translateY(-20px); /* Augmenter le mouvement vers le haut */
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.25); /* Ombre plus grande et plus nette */
            border: 3px solid #053536; /* Bordure plus épaisse et colorée */
        }

        .custom-image {
            border-radius: 0;  /* Supprime la bordure arrondie */
        }
    </style>
{% endblock %}
